## 1. 基线与测试夹具

- [ ] 1.1 建立可复用的大型项目夹具，生成包含大量 Markdown 文件和多层目录的临时项目，并验证夹具节点数、层级和排序输入可重复
- [ ] 1.2 增加初始项目扫描计数探针，记录 `loadMarkdownFile` 调用次数、`mt::update-object-tree` 发送次数、Renderer 树提交次数和已挂载树行数，并验证探针只用于测试且不改变生产行为
- [ ] 1.3 记录当前分支在大型项目夹具下的初始打开时间、IPC 次数、读取次数、挂载行数和侧边栏拖拽更新次数作为基线，并验证基线结果可由同一命令重复产出

## 2. 初始元数据扫描与按需内容加载

- [x] 2.1 调整目录 watcher 的初始 `add`/`addDir` 路径，使其只发送项目树元数据并跳过 `loadMarkdownFile()`，通过单元测试验证初始扫描阶段没有任何 Markdown 正文读取
- [x] 2.2 明确并实现项目树元数据契约（至少覆盖 `pathname`、`name`、`isDirectory`、`isFile`、`isMarkdown`、`birthTime`、`mtimeMs`），通过类型检查和 watcher 单元测试验证 Renderer 建树、创建时间和修改时间排序所需字段完整
- [ ] 2.3 保留用户打开文件时的按需内容加载路径，通过现有文件打开与编码、BOM、混合换行、trailing newline 测试验证正文、换行和错误语义不变
- [ ] 2.4 保留文件 watcher 的 `mt::update-file` 内容更新路径，通过外部修改 Markdown 文件的测试验证已打开文件获得最新正文且未打开文件不被读取
- [ ] 2.5 保留新建文件首次加载内容的特殊路径，通过侧边栏创建文件后立即进入重命名/编辑状态的测试验证内容初始化不丢失

## 3. 初始快照、有界批次与增量合并

- [x] 3.1 在目录 watcher 中收集初始发现结果，并在 ready 后发送一个项目树快照或固定大小批次，通过 watcher 单元测试验证初始 IPC 次数不随节点数一一增长
- [ ] 3.2 为初始扫描期间的 add、unlink、addDir、unlinkDir 和 change 建立有界增量队列，通过并发创建、删除和修改测试验证最终树与文件系统一致
- [ ] 3.3 实现按 pathname 的确定性合并与重放规则，通过同一路径快速 add/unlink/change 的测试验证最终状态为最新状态且无重复节点
- [ ] 3.4 在 Renderer 中使用普通对象构建和排序初始树，完成后一次提交 `projectTree`，通过 store 测试验证初始提交次数不随节点数线性增长
- [ ] 3.5 处理初始快照早于项目恢复或打开到达的情况，通过先收到快照再执行 `RESTORE_BUFFERED_STATE` / `OPEN_PROJECT` 的测试验证快照应用到正确项目根
- [ ] 3.6 处理切换项目时旧快照和旧增量事件的隔离，通过快速切换两个项目根并延迟投递事件的测试验证新树不混入上一项目节点
- [ ] 3.7 验证快照应用后后续 watcher 事件仍按既有顺序生效，通过初始加载完成后新增、删除和修改文件的集成测试验证树、排序和活动文件状态正确

## 4. 扁平可见行与虚拟文件树

- [x] 4.1 从逻辑项目树派生可见行数组，包含节点引用、深度、类型和展开状态，通过单元测试验证展开/折叠、排序和空目录对应的行序列正确
- [x] 4.2 为可见行提供稳定标识和逻辑索引映射，通过重命名、排序变化和插入删除测试验证选中、展开和滚动锚点不跳到错误节点
- [x] 4.3 实现基于固定行高的虚拟列表，只挂载视口及预加载范围内的行，通过包含数千子节点的夹具验证挂载行数保持有界
- [x] 4.4 接入滚动、展开、折叠、单选和打开文件交互，通过 e2e 或组件测试验证任意滚动位置的可见行与逻辑节点一致
- [x] 4.5 将活动文件、键盘导航和程序化定位滚动到视口外节点，通过跳转测试验证目标行滚入视口并保持选中和焦点
- [x] 4.6 处理创建和重命名输入行在虚拟化下的挂载、定位和提交，通过根目录和深层目录创建/重命名测试验证输入不指向错误节点且提交结果正确
- [x] 4.7 将上下文菜单锚点和活动节点绑定到逻辑行，通过文件、文件夹和空白区域右键测试验证菜单内容、位置和文件操作目标不变
- [x] 4.8 验证文件树总滚动高度与滚动位置可覆盖全部逻辑行，通过滚动到底部并检查最后节点可见的测试验证没有截断或空白区

## 5. 事件监听与拖拽更新降载

- [x] 5.1 将创建和重命名通知从每个文件夹/文件节点迁移到树容器集中注册，通过组件测试验证全局 bus 监听数量不随节点数增长
- [x] 5.2 通过活动路径或逻辑节点状态把集中通知路由到唯一目标，通过单节点重命名和创建测试验证其他节点不显示输入框且无重复处理
- [x] 5.3 将文件树上下文菜单改为容器事件委托并移除节点级 `contextmenu` 监听，通过右键菜单测试和监听器数量断言验证行为不变
- [x] 5.4 评估并减少节点对 Pinia 状态的重复订阅，通过大型可见树测试验证活动文件、排序和展开状态更新不会触发无界重复渲染
- [x] 5.5 在侧边栏拖拽中使用 `requestAnimationFrame` 合并宽度写入，并在 `mouseup` 提交最终宽度，通过拖拽测试验证每帧最多一次更新且最终宽度正确
- [ ] 5.6 保持侧边栏最小宽度和持久化行为，通过拖到最小值、释放、重新挂载和切换侧栏内容的测试验证宽度不丢失或回退

## 6. 集成回归与性能验证

- [ ] 6.1 在大型项目夹具上运行初始加载测试，验证 `loadMarkdownFile` 初始调用为 0、IPC 发送次数远小于节点数、树提交次数有界且最终树与文件系统一致
- [ ] 6.2 在大型展开目录上运行挂载行数测试，验证 DOM/组件行数与视口高度成比例而非常量阈值以上的线性增长
- [ ] 6.3 运行文件树操作回归，覆盖排序、创建、重命名、删除、复制粘贴、打开文件、折叠展开和窗口切换，验证行为与错误反馈符合 spec
- [ ] 6.4 运行扫描期间变更、项目切换和初始加载后外部变更测试，验证事件不丢失、不重复、不跨项目串扰且排序与活动状态正确
- [ ] 6.5 对比优化前后基线，记录初始打开时间、正文读取次数、IPC 次数、树提交次数、挂载行数、拖拽帧更新次数和内存样本，验证收益可量化且无回归切片
- [ ] 6.6 运行 `corepack pnpm --filter marktext typecheck`、受影响 Vitest 测试和相关 Playwright e2e，验证类型检查、单元测试和交互回归全部通过
- [ ] 6.7 检查 IPC/preload 安全边界和项目树数据结构兼容性，验证 Renderer 未获得新的文件系统能力且旧状态恢复路径仍然可用
- [ ] 6.8 记录每个性能切片的提交、测量结果和回滚方式，验证出现行为或性能回归时可以只回滚对应切片

<!-- Implementation note (2026-09-15)

已完成 2.1、2.2、3.1：初始目录扫描改为元数据快照，单次 IPC；Renderer 单次建树提交并缓存快照。

已完成 4.1-4.8：侧边栏改为“逻辑树 + 扁平可见行 + 固定行高虚拟列表”。新增 `visibleRows.ts`（纯函数）与 `treeRow.vue`，`tree.vue` 只挂载视口及 overscan 范围内的行，滚动容器用 spacer 反映全部逻辑行高度；展开/折叠、创建/重命名输入行、活动文件定位、右键菜单锚点均按 pathname 路由。

已完成 5.1-5.4：创建/重命名通知由树容器单点订阅，行通过 focusRegistry 按 row key 注册处理函数；上下文菜单改为容器 `data-pathname` 事件委托，行组件不再持有 DOM 监听；行组件不再使用 storeToRefs，仅容器读取 Pinia 状态。

已完成 5.5：侧边栏拖拽宽度按 requestAnimationFrame 合并写入，mouseup 提交最终宽度。

验证：`vue-tsc` 通过；`vitest run test/unit`（106 文件 / 993 用例）通过；新增 sidebar-virtual-rows 用例覆盖行序列、稳定 key、虚拟窗口边界、祖先展开；`electron-vite build` 通过。

尚未完成：1.x 大型项目夹具与基线、2.3-2.5 与 3.2-3.7 的针对性回归用例、5.6 拖拽最小宽度与持久化用例、6.x 全量回归与基线对比。

环境限制：本机 `ced` 原生模块未编译（无 Visual Studio），Electron 加载即失败，因此 Playwright e2e 未能执行；6.x/6.6 的 e2e 需要在可构建原生模块的环境补跑。
-->
